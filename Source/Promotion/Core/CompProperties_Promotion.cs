using Verse;

namespace Promotion
{
    public class CompProperties_Promotion : CompProperties
    {
        public CompProperties_Promotion()
        {
            compClass = typeof(PawnPromotionComp);
        }
    }
}